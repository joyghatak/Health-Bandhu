# HealthBandhu V: web app requirements
# Install with:  pip install -r requirements.txt
streamlit>=1.50,<2
tensorflow>=2.16          # loads the .keras deep learning model (Keras 3)
numpy>=1.26,<3
pandas>=2.1
altair>=5
reportlab>=4.0            # PDF clinical reports
joblib>=1.3               # only used to read legacy .pkl symptom and disease lists
